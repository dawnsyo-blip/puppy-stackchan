firmware/config.h
*.pkl
*.jpg
*.wav
__pycache__/
.DS_Store

# README 演示视频源文件——体积大（几十MB），实际托管在 GitHub Release
# （demo-videos tag）里，不进仓库历史，见 README.md "演示视频" 一节
*.MOV
*.mov

# API keys / secrets — see .env.example for the required variable names
.env

# 内部开发笔记，只留本地参考——含 WiFi 密码等不该公开的内容，也不是外部
# 使用者需要看的东西（见 README.md）
CLAUDE.md

# 个人参考资料/设计文档，不是仓库内容（见 CLAUDE.md）：捉迷藏/表情设计的
# 权威参考表格、项目路线图笔记、下载的教程 PDF、Mermaid 状态机图源文件+渲染
# 产物（命名细节偏内部实现，只留本地参考，不随仓库公开）
*.xlsx
project_roadmap.md
*.pdf
mmd/

# MediaPipe 模型文件是本地依赖，不提交（体积大，且能重新下载，见 CLAUDE.md
# "手指枪手势检测"一节）
host/hand_landmarker.task

# 已被 puppy_engine_v4.py 取代的旧版本，只是本地留存，不进仓库
host/puppy_engine.py
host/puppy_engine_v2.py
host/puppy_engine_v3.py

# 独立诊断/测试脚本——按 CLAUDE.md 里记录的既定惯例，这些脚本只在本机验证
# 用，不进仓库（跟 voice_test.py 那次的惯例一致）
host/voice_test.py
host/gesture_test.py
host/reminder_test_driver.py
host/animalese_test.py
host/test_hardware.py
host/stackchan_watch.py
